# TripDiary — Claude Code Instructions

## スクリーンショット

動作確認で撮影したスクリーンショットは必ず `screenshots/` フォルダに保存すること。
ルートディレクトリや `.playwright-mcp/` などに直接保存しないこと。

Playwright MCP で撮影した場合は、撮影後に `screenshots/` へ移動すること。

```
screenshots/   ← すべての動作確認スクリーンショットをここに置く
```
